import{_ as a,o as s,c as i,a2 as n}from"./chunks/framework.CHeM0PsO.js";const h=JSON.parse('{"title":"Music HUD - Agent Guide","description":"","frontmatter":{},"headers":[],"relativePath":"projects/music-hud-paper/AGENTS.md","filePath":"projects/music-hud-paper/AGENTS.md"}'),o={name:"projects/music-hud-paper/AGENTS.md"};function r(c,e,d,t,l,p){return s(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="music-hud-agent-guide" tabindex="-1">Music HUD - Agent Guide <a class="header-anchor" href="#music-hud-agent-guide" aria-label="Permalink to &quot;Music HUD - Agent Guide&quot;">​</a></h1><p>Multi-loader Minecraft mod (Fabric + NeoForge + Paper) for 1.21.8 (Java 21). A GUI-based full-server song request system powered by Netease Cloud Music API.</p><h2 id="build-run" tabindex="-1">Build &amp; Run <a class="header-anchor" href="#build-run" aria-label="Permalink to &quot;Build &amp; Run&quot;">​</a></h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Build specific loader (produces shadowed fat jars):</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./gradlew</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fabric:build</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # build/libs/music_hud-fabric-&lt;version&gt;.jar</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./gradlew</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> neoforge:build</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # build/libs/music_hud-neoforge-&lt;version&gt;.jar</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./gradlew</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> paper:build</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           # build/libs/music_hud-paper-&lt;version&gt;.jar</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Run (Fabric/NeoForge via Loom):</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./gradlew</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fabric:runClient</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./gradlew</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> neoforge:runClient</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./gradlew</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> fabric:runServer</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">./gradlew</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> neoforge:runServer</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><h2 id="critical-quirks" tabindex="-1">Critical Quirks <a class="header-anchor" href="#critical-quirks" aria-label="Permalink to &quot;Critical Quirks&quot;">​</a></h2><ul><li><strong>Tests are DISABLED by default</strong> (<code>enabled = false</code> in <code>common/build.gradle:46</code>). To run: edit the file and change <code>enabled</code> to <code>true</code> — there is no Gradle property to override this at runtime.</li><li><strong>No CI, no linter, no formatter, no typechecker</strong> configured. Do not look for or run these.</li><li><strong>Paper module is separate</strong> — uses <code>paperweight.userdev</code> directly, NOT Architectury Loom. Paper is NOT in <code>settings.gradle</code> on this branch.</li><li><strong>Build scripts are Groovy DSL</strong> (<code>.gradle</code>), not Kotlin (<code>.gradle.kts</code>).</li><li><strong><code>core</code> module is a plain <code>java-library</code> (NO Loom)</strong> — excluded by <code>build.gradle:16</code>: <code>configure(subprojects.findAll { it.name != &#39;core&#39; })</code>. <ul><li>Platform modules (<code>fabric</code>/<code>neoforge</code>) add <code>core</code> via a dedicated <code>coreLib</code> configuration that extends <code>compileClasspath</code> + <code>runtimeClasspath</code> but <strong>NOT</strong> <code>developmentFabric</code>/<code>developmentNeoForge</code> — this prevents Architectury Transformer from applying unnecessary transforms (<code>GenerateFakeFabricMod</code>, <code>RemapInjectables</code>).</li></ul></li><li><strong><code>-Dfabric.dli.config</code> must be overridden in Fabric <code>loom.runs</code></strong> — Architectury Loom 1.17.487 generates DLI config at <code>.gradle/loom-cache/projects/&lt;subproject&gt;/launch.cfg</code> but the injector property points to <code>&lt;subproject&gt;/.gradle/loom-cache/launch.cfg</code>. Without the override, <code>dev-launch-injector</code> enters pass-through mode → mods and Minecraft assets won&#39;t load.</li><li><strong>Mixin count: 6</strong> — <code>SoundEngineMixin</code>, <code>GuiRendererHudMixin</code>, <code>ScreenMixin</code>, <code>SpanSetMixin</code>, <code>ReactiveMusicCompatMixin</code> + <code>MusicHudMixinPlugin</code> (plugin class).</li></ul><h2 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h2><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>core/            — platform-independent Java library (no Minecraft deps)</span></span>
<span class="line"><span>                   network codecs, payload interfaces, data beans, JMTC (SMTC/MPRIS),</span></span>
<span class="line"><span>                   server API interfaces, utility classes (RegistrationManager, etc.)</span></span>
<span class="line"><span>common/          — Architectury common module with Minecraft deps</span></span>
<span class="line"><span>                   UI (ModernUI), audio (stream decoders), mixins, platform service impls</span></span>
<span class="line"><span>fabric/          — Fabric loader adapter layer (shadow-jars common + core)</span></span>
<span class="line"><span>neoforge/        — NeoForge adapter layer (shadow-jars common + core)</span></span>
<span class="line"><span>paper/           — Paper/Bukkit plugin (on a separate branch, not in settings here)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p><code>common</code> depends on <code>core</code> (<code>api project(&#39;:core&#39;)</code> in <code>common/build.gradle:24</code>). Platform modules shadow both <code>common</code> and <code>core</code> via <code>shadowBundle</code>.</p><p>The <code>configure(subprojects.findAll { it.name != &#39;core&#39; })</code> block in <code>build.gradle:16</code> applies Loom to <code>common</code>, <code>fabric</code>, <code>neoforge</code> — but NOT <code>core</code>.</p><h2 id="key-patterns" tabindex="-1">Key Patterns <a class="header-anchor" href="#key-patterns" aria-label="Permalink to &quot;Key Patterns&quot;">​</a></h2><ul><li><strong>Service Locator</strong> (not DI): <code>Environment.Platform.load()</code> uses <code>Class.forName()</code> + reflection to load platform-specific implementations. Interfaces: <code>ServerConfig</code>, <code>ClientConfig</code>, <code>IClientEventService</code>, <code>IServerEventService</code>, <code>INetworkRegister</code>, <code>IKeyRegistryService</code>.</li><li><strong>Auto-Registration</strong>: <code>RegistrationManager</code> loads classes by string array, instantiates <code>Register</code> implementors. <code>@RegisterMark</code> annotation marks registered classes. Called via <code>performCommonAutoRegistration()</code> / <code>performSideAutoRegistration()</code>.</li><li><strong>Custom Network Protocol</strong>: 30+ <code>CustomPacketPayload</code> classes in <code>network/payloads/</code>. Request/response cycle (<code>requestResponseCycle/</code>) + push messages (<code>pushMessages/</code>). Register via <code>INetworkRegister</code>.</li><li><strong>Virtual Threads</strong>: <code>MusicHud.EXECUTOR</code> = <code>Executors.newVirtualThreadPerTaskExecutor()</code>. Used for audio decoding, API server management, network I/O.</li><li><strong>Mixin</strong>: Config <code>music_hud.mixins.json</code>. 6 classes: <code>SoundEngineMixin</code>, <code>GuiRendererHudMixin</code>, <code>ScreenMixin</code>, <code>SpanSetMixin</code>, <code>ReactiveMusicCompatMixin</code> + <code>MusicHudMixinPlugin</code>.</li><li><strong>Config</strong>: Forge Config API Port (Fabric, shared with NeoForge) / native NeoForge <code>ModConfig</code> / Paper <code>config.yml</code>.</li></ul><h2 id="frameworks-dependencies" tabindex="-1">Frameworks &amp; Dependencies <a class="header-anchor" href="#frameworks-dependencies" aria-label="Permalink to &quot;Frameworks &amp; Dependencies&quot;">​</a></h2><ul><li><strong>ModernUI</strong> (icyllis.modernui) — GUI framework, NOT vanilla MC widgets. Flat JARs in <code>libs/</code>.</li><li><strong>Lombok</strong> heavily used — annotation processing is already configured.</li><li><strong>JLayer</strong> (MP3) + <strong>JFLAC</strong> (FLAC) — audio decoders, shaded into output jars.</li><li><strong>Mojang mappings</strong> — <code>loom.officialMojangMappings()</code>.</li></ul><h2 id="external-dependencies" tabindex="-1">External Dependencies <a class="header-anchor" href="#external-dependencies" aria-label="Permalink to &quot;External Dependencies&quot;">​</a></h2><ul><li><strong>NCM API Enhanced</strong> — external process (<code>api</code>/<code>api.exe</code>) managed by <code>ApiServerManager</code>. Lifecycle: auto-start on launch, manages process I/O via virtual threads. Deploy to <code>{corepath}/music-hud/</code>.</li></ul><h2 id="testing" tabindex="-1">Testing <a class="header-anchor" href="#testing" aria-label="Permalink to &quot;Testing&quot;">​</a></h2><ul><li>JUnit Jupiter 6.0.0.</li><li>Single test class: <code>common/src/test/java/indi/etern/musichud/MainTest.java</code>.</li><li>Tests call live NCM API (network-dependent, may fail offline).</li><li><strong>Must enable manually</strong> before running tests.</li></ul><h2 id="misc" tabindex="-1">Misc <a class="header-anchor" href="#misc" aria-label="Permalink to &quot;Misc&quot;">​</a></h2><ul><li>Languages: <code>en_us</code>, <code>zh_cn</code>, <code>zh_hk</code>, <code>zh_tw</code>.</li><li>Custom GL shaders (<code>.vsh</code>/<code>.fsh</code>) for album cover, progress bar, fluid background.</li><li><code>gradle.bac/</code> is a backup of a previous Gradle wrapper — not the active one (<code>gradle/</code> is live).</li></ul>`,20)])])}const g=a(o,[["render",r]]);export{h as __pageData,g as default};
